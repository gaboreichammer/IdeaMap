export const environment = {
  production: true,
  // Your containerized/cloud base URL
  // Note: Since Angular will be served from the same domain as Spring Boot,
  // we often use a relative path or the domain itself.
  apiBaseUrl: 'https://api.yourcloudapp.com/api',
};
